@model SatoshisMarketplace.Web.Models.Admin.CreateTagViewModel
@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Create Tag Page";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card">
            <div class="card-header">
                Create Tag
            </div>
            <div class="card-body">
                <form method="post" asp-action="CreateTag" asp-controller="Admin" enctype="multipart/form-data">
                    <div class="mb-3">
                        <label for="DisplayName" class="form-label">Display Name</label>
                        <input type="text" class="form-control" id="DisplayName" name="DisplayName" required>
                    </div>
                    <div class="mb-3">
                        <label for="Description" class="form-label">Description</label>
                        <textarea class="form-control" id="Description" name="Description" rows="3" maxlength="500" required></textarea>
                    </div>
                    <button type="submit" class="btn btn-outline-primary">Create Tag</button>
                </form>
            </div>
        </div>
    </div>
</div>